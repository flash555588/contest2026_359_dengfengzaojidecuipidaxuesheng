# 当前任务与开发缺口（2026-09-16）

当前交付为 [espdl-quickapp](04-v3-20260913/espdl-quickapp/README.md)，本次固件 SHA256 `60039046a1784473ce11e6c503bace298077b398fc5df8cf169a3a19e3dc80ba`。完整 AI 快应用指导、三个可读取的验证示例、流式回复／思考状态、聊天刷新优化，以及捕获到的闪存缓存关闭期间取指异常修复均已集成。增加放弃创意、版本归零和非系统快应用删除；动态分配应用列表、控件、事件、定时器，去除固定快应用源码／包大小配额。测试和实板证据见 [稳定性与交互记录](04-v3-20260913/espdl-quickapp/CHAT-STABILITY.md)。本次只更新程序区，保留模型及现有用户数据。以下历史任务的“通过”仅指各自记录的测试范围。

| 历史任务 | 状态 | 已完成与验证范围 |
| --- | --- | --- |
| 三组历史源码与最新桌面比较 | 已完成本轮排障所需对比 | 旧源码、配置、构建记录分别保留；并非所有旧版本都已建立精确源码／二进制对应关系 |
| 烧录后黑屏 | 已修复并烧录 | GDMA 跨堆释放、P4 Simple Boot 摘要与软浮点运行库问题已处理；NSH、显示驱动和实板帧缓冲验证通过 |
| 编入软件测试 | 部分完成 | NSH、存储、ESPClaw 离线自检及 C6 初始化有实测；外部聊天服务、音频和相机不能据“已编入”判为功能通过 |
| Wi-Fi 联网／DHCP | 已修复并实测 | 对比 WSL 原版后关闭有线 EMAC／自动联网，修复 eth0 冲突；关联与 DHCP 成功，曾取得 192.168.0.105 |
| 应用显示存储无效 | 已修复并实测 | 按 SmartFS 文件名／目录深度限制分段存储，兼容旧格式；覆盖、读回、删除与边界测试通过，未格式化数据分区 |
| 控件排版、亮度条、Wi-Fi“密码”方块 | 已修复指定页面 | 27 个页面／主题状态有 UI 包截图与坐标审计；中文输入样式、弹窗、HA、Hello、2048 等已调整。当前硬件亮度仍固定，未实现 PWM 调光 |
| 早先相机未接导致的排查 | 按用户更正撤销该修复方向 | 用户确认当时未连接相机；后续 BLE 两个交付包未改相机。连接后的真实取景尚未验收 |
| BLE 启动／扫描 | 已修复并实测 | socket 锁、HCI 设备选择和封包、网络堆释放、扫描定时器已修复；自动结束、重复扫描、关闭取消以及 Wi-Fi 共存通过 |
| BLE 设备名称 | 已实现并烧录 | 主动扫描并合并完整／缩略名称，命名设备优先，两行列表；真实显示 EDIFIER BLE，最终扫描 16 个设备，列表上限 32 |
| 蓝牙耳机／音箱配对放音 | 尚未完成，受硬件限制 | C6 不支持经典蓝牙 A2DP；需 A2DP Source 发射模块或原版 ESP32，尚缺模块和目标耳机型号。接入说明已交付 |
| 相机设备数量、CSI＋USB、多设备切换 | **USB 单机基本功能通过，兼容验证未完成** | 实测 HD Web Camera，CSI 0／USB 1，6 档 MJPEG／协商约 30 fps。640×480 取景、JPEG 保存与独立解码、反复开停已通过；显示约 6 fps。第 11 版修复真实缓存写回。多设备 UI 和解析器测试通过，不代表多机、CSI、高分辨率实测通过 |

用户已更换工作地点并确认原热点消失。后续关联／DHCP 失败按环境变化记录，不作为固件回归；最新名称版未再次尝试 DHCP。

## 未来开发缺口

ESP-DL 当前范围已收敛为[人脸跟随与物体分类](04-v3-20260913/espdl-quickapp/README.md)。已接入官方 P4 向量算子和硬件循环上下文保护，主机跟踪/取消测试及加速版构建通过；正在实板验证。该开发版改用 8 MiB 程序区、4 MiB 模型区和位于 `0xc00000` 的 4 MiB `/data`，用户已授权清除旧数据。早期行人和数字识别实验不属于当前交付范围。

ESP-Claw 语音输入已开始开发，见 [开发状态](04-v3-20260913/espclaw-voice/README.md)。
录音／转写／提问和桌面入口已有源码，宿主封装及流程测试通过；当前镜像超出程序分区，
该独立语音版未烧录，未采集真实音频，未接通真实转写服务。当前实板固件以本文顶部和最新实板证据为准。

| 优先级 | 工作 | 完成条件或依赖 |
| --- | --- | --- |
| 当前 | CSI／USB 相机应用 | 设备选择页已实板截图验证；继续完成反复取景／退出、原分辨率 JPEG、拍照中取消、1080p 和多设备切换实测。CSI 当前未接，仅启动探测成功才列出 |
| 当前 | P4 USB Host 与 UVC | 已移植独立 CherryUSB HS Host 和自写 UVC，配置新增 `CONFIG_ESP32P4_CAMERA_USB=y`；原 NuttX `CONFIG_USBHOST` 仍关闭。已通过单机开停，仍需热插拔、Hub、多机及长期恢复验证；全速等时及其他间隔尚未支持 |
| 下一阶段 | 蓝牙耳机／音箱 | 选定支持 A2DP Source 的硬件；实现发现、经典名称查询、配对绑定、重连、PCM 路由、音量和连续播放验收 |
| 后续 | BLE 连接与配对 | 指定可测试外设，验证连接／断开、认证、绑定持久化和 GATT；扫描成功不代表这些项目通过 |
| 后续 | 本机音频／麦克风 | 复核输入输出节点，验证实际录音与有线放音、格式转换和播放器控制；当前只有早期输入节点及界面证据 |
| 后续 | 实际亮度调节 | 确认背光控制硬件与引脚，再启用 PWM／驱动；目前 UI 明示固定亮度 |
| 后续 | 联网稳健性 | 新热点下复测、断线重连、DHCP 续租、DNS、外网 HTTP/TLS；历史 DHCP 成功不等于外网全部通过 |
| 后续 | HA／聊天服务 | 聊天已实际完成流式思考、回答与工具调用；继续长期网络恢复验证。HA 实体控制仍需独立验收 |
| 后续 | 长期和外设回归 | 触摸准确性、SD 卡、第三方 QPK、长时间负载、断电恢复和音视频／无线共存 |
| 工程化 | 独立可复现构建与证据归属 | 当前脚本仍依赖隔离构建树、固定工具链与缓存；需整理从原始归档一键构建流程。旧 UI 两份误归属日志已更正，后续证据须绑定实际固件哈希 |

## 交付入口

- [黑屏修复](04-v3-20260913/black-screen-fix/README.md)
- [Wi-Fi DHCP 修复](04-v3-20260913/wifi-dhcp-fix/README.md)
- [应用存储修复](04-v3-20260913/app-storage-fix/README.md)
- [桌面排版修复](04-v3-20260913/ui-layout-fix/README.md)
- [BLE 启动与扫描修复](04-v3-20260913/ble-startup-fix/README.md)
- [当前蓝牙名称版](04-v3-20260913/ble-device-names/README.md)
- [当前相机测试版与未完成项](04-v3-20260913/camera-usb-fix/README.md)
- [蓝牙音频接入要求](04-v3-20260913/ble-device-names/AUDIO-INTEGRATION.md)
