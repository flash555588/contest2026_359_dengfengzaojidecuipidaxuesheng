# 中间版本测试记录，不用于烧录

SHA-256 `58227942b860e7465ace80d8876696226cfd32a553bc21f6c09a3c0acee7f47e` 的中间版本在实板最长名称测试中返回 `-36`，未满足交付条件。

后续修复将名称安全长度限制为 31 字符，并移除独立的 `@2` 目录层。最终通过测试的镜像位于上两级目录，SHA-256 为 `2159a0adca9a7e76680233064d907aa3676b6d10da12b1e35dae3901f7b65932`。

本次失败留下的独占测试目录已通过非递归 `rmdir` 清理，见 `cleanup.json`。没有操作旧应用数据。
