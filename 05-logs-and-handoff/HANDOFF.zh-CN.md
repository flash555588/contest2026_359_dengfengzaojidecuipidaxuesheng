# V3 源码与日志交接

交接日期：2026-09-14。本说明对应桌面 `wdddd/v3-sources-20260914` 源码集合。
本次仅整理本地已有文件，没有重新编译、烧录或进行实板验收。

## 接手入口

先阅读根目录 README.md，再查看所选版本的源码和本目录同名分组的日志。
大源码树保存在 tar.gz 中，关键桌面和应用源码已单独展开。
必须在独立目录解压不同版本；不要把四组文件直接叠加。
Linux 符号链接、绝对路径和外部依赖可能仍指向原 WSL 环境。

## 四组版本的边界

`01-v3.2-pinned` 的 NuttX 和 Apps 来自元数据记录的固定提交。
附带项目源码为当前参考版本，不证明与历史候选固件完全对应。
本次补充 2026-08-27 的 v3.2 构建日志。编译成功不等于 v3.2 实板验收。

`02-v3plus` 包含 2026-09-06 v3.0+ 隔离桌面构建树及独立应用快照。
日志保留 isolated-v3、isolated-v3plus 和 isolated-current-v3plus 原始路径，
分别辨别 301-399、300-399 修订范围以及独立应用变体，不混用配置。
原始说明将隔离候选标为未硬件验收。

`03-early-v3` 包含恢复出的早期桌面、运行时、板级源码和当前参考仓库。
新增 esp32p4_v3_desktop 下的启动、交互日志；目录名称不能证明实际芯片版本。
历史移植记录明确当时连接的是 v1.0 板，不能把这些记录当作 v3 实板成功证据。
尚未建立旧固件与完整历史源码的严格对应关系。

`04-v3-20260913` 是昨天建立、今天仍有构建活动的工作树当前源码快照。
日志按资源管理、生命周期、相机、BLE、聊天和麦克风等原产物目录分开保存。
主机测试及 ASan 记录不是硬件验收，也不是网络服务端到端成功证据。
聊天候选说明没有真实外部模型请求；麦克风候选说明尚无真实录音验收。
不能将当前源码树称作 9 月 13 日最终固件的不可变历史快照。

## 历史交接文档

historical-handoff 内保存三份既有交接文档及两份移植状态记录。
CAMERA_HANDOFF_20260831.md 属于历史相机工作背景。
ESP32P4_OPENVELA_V3_NSH_HANDOFF_20260907.md 属于独立最小 NSH 移植路线。
OPENVELA_P4_HANDOFF_20260907.md 包含后续增量和多个历史快照。
这些文件的计划和“最新”描述仅代表其写作时状态，不覆盖当前用户决定，
也不意味着其中引用的全部源码、工具链和固件已包含在本集合中。

## 日志来源与校验

LOG-MANIFEST.json 逐项记录原始绝对路径、导出相对路径、两端 SHA-256 和脱敏标记。
仅补充工程日志及相关产物说明，没有归集 AI 会话 JSONL。
导出前对常见凭据格式进行自动脱敏；自动检测不保证识别所有敏感信息，
对外分享前仍须检查网络地址、设备信息和业务内容。
未脱敏文件按字节复制；被脱敏文件保留原始文件不变，并记录不同摘要。
根目录 SHA256SUMS 已更新以覆盖新增交接和日志文件。

## 后续工作

先确定目标板芯片修订、Flash/PSRAM 和控制台，再选择唯一基线。
核对源码提交、实际配置、HAL 与工具链依赖后执行独立干净构建。
对昨天版本先固定源码快照并重建，建立源码、配置、ELF 和 BIN 的对应关系。
实板测试必须另外保存芯片识别、冷启动及各功能验收记录。
不要仅凭本次文件完整性校验批准烧录或宣称功能已验证。
